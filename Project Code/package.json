{
    "name": "node-ejs",
    "main": "server.js",
    "dependencies": {
        "bcrypt": "^5.0.1",
        "chai-http": "^4.3.0",
        "connect-flash": "^0.1.1",
        "ejs": "^3.1.6",
        "ejs-lint": "^1.1.0",
        "express": "^4.17.1",
        "express-session": "^1.17.2",
        "mocha": "^9.2.2",
        "nodemon": "^2.0.15",
        "passport": "^0.5.2",
        "passport-local": "^1.0.0",
        "pg-promise": "^10.7.0",
        "session-file-store": "^1.5.0",
        "uuid": "^8.3.2"
    },
    "scripts": {
        "prestart": "npm i connect-flash && npm i bcrypt && npm i chai-http && npm install --save-dev chai && npm install mocha --save && npm i npm-run-all -D && npm install --save uuid && npm install express-session --save && npm install session-file-store --save && npm install passport passport-local --save",
        "start": "node server.js",
        "dev": "nodemon server.js"
    },
    "devDependencies": {
        "chai": "^4.3.6",
        "npm-run-all": "^4.1.5"
    }
}
